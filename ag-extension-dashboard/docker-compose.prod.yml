# ============================================================
# Production Override — HTTPS + Let's Encrypt + Nginx Frontend
# ============================================================
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml \
#     -f docker-compose.agents.yml up -d --build
#
# Ensure these are set in .env:
#   ACME_EMAIL=admin@gpexts.com     (required for Let's Encrypt)
#   DOMAIN=www.gpexts.com           (your production domain)
#
# Architecture:
#   www.gpexts.com:443 ─→ Traefik ─→ frontend:80 (Nginx, SPA)
#   gpexts.com:443     ─→         ─→ backend:3001 (API)
#   HTTP :80 redirects to HTTPS :443
#
# Note: Let's Encrypt cert covers both www.gpexts.com and gpexts.com
# via tls.domains SAN configuration.
# ============================================================
#
# IMPORTANT: Docker Compose v5 replaces (not merges) the `labels:` list
# from the base compose file. This override MUST include ALL required
# labels including traefik.enable and loadbalancer.server.port.

services:
  traefik:
    image: traefik:v3.7
    command:
      # Docker provider
      - "--providers.docker=true"
      - "--providers.docker.exposedByDefault=false"
      - "--providers.docker.network=ag-network"
      # HTTP entrypoint (no global redirect — handled by router-level redirect)
      - "--entrypoints.web.address=:80"
      # HTTPS entrypoint with Let's Encrypt
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.letsencrypt.acme.tlschallenge=true"
      - "--certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL}"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik_letsencrypt:/letsencrypt
    networks:
      - ag-network
    restart: always

  backend:
    labels:
      # Enable Traefik discovery (must be explicit — labels list replaces base)
      - "traefik.enable=true"
      # Service port for load balancing
      - "traefik.http.services.backend.loadbalancer.server.port=3001"
      # Router 1: API domain on HTTP → HTTPS redirect
      - "traefik.http.routers.backend-redirect.rule=(Host(`${DOMAIN:-www.gpexts.com}`) || Host(`gpexts.com`)) && (PathPrefix(`/api`) || PathPrefix(`/api-docs`) || PathPrefix(`/socket.io`) || PathPrefix(`/health`))"
      - "traefik.http.routers.backend-redirect.entrypoints=web"
      - "traefik.http.routers.backend-redirect.middlewares=https-redirect"
      # Router 2: API domain on HTTPS + Let's Encrypt TLS
      - "traefik.http.routers.backend.rule=(Host(`${DOMAIN:-www.gpexts.com}`) || Host(`gpexts.com`)) && (PathPrefix(`/api`) || PathPrefix(`/api-docs`) || PathPrefix(`/socket.io`) || PathPrefix(`/health`))"
      - "traefik.http.routers.backend.entrypoints=websecure"
      - "traefik.http.routers.backend.tls.certresolver=letsencrypt"
      - "traefik.http.routers.backend.tls.domains[0].main=www.gpexts.com"
      - "traefik.http.routers.backend.tls.domains[0].sans=gpexts.com"
      # Router 3: localhost/IP on HTTP only (for health checks / debugging)
      - "traefik.http.routers.backend-local.rule=(Host(`localhost`) || Host(`145.223.97.248`)) && (PathPrefix(`/api`) || PathPrefix(`/api-docs`) || PathPrefix(`/socket.io`) || PathPrefix(`/health`))"
      - "traefik.http.routers.backend-local.entrypoints=web"
      # HTTP→HTTPS redirect middleware (defined per-container for Docker provider scope)
      - "traefik.http.middlewares.https-redirect.redirectscheme.scheme=https"

  frontend:
    build:
      context: ./src/frontend
      target: production
    labels:
      # Enable Traefik discovery (must be explicit — labels list replaces base)
      - "traefik.enable=true"
      # Service port for load balancing (Nginx serves on 80)
      - "traefik.http.services.frontend.loadbalancer.server.port=80"
      # Router 1: domain on HTTP → HTTPS redirect
      - "traefik.http.routers.frontend-redirect.rule=Host(`${DOMAIN:-www.gpexts.com}`) || Host(`gpexts.com`)"
      - "traefik.http.routers.frontend-redirect.entrypoints=web"
      - "traefik.http.routers.frontend-redirect.middlewares=https-redirect"
      # Router 2: domain on HTTPS + Let's Encrypt TLS
      - "traefik.http.routers.frontend.rule=Host(`${DOMAIN:-www.gpexts.com}`) || Host(`gpexts.com`)"
      - "traefik.http.routers.frontend.entrypoints=websecure"
      - "traefik.http.routers.frontend.tls.certresolver=letsencrypt"
      - "traefik.http.routers.frontend.tls.domains[0].main=www.gpexts.com"
      - "traefik.http.routers.frontend.tls.domains[0].sans=gpexts.com"
      # Router 3: localhost/IP on HTTP only (for health checks / debugging)
      - "traefik.http.routers.frontend-local.rule=Host(`localhost`) || Host(`145.223.97.248`)"
      - "traefik.http.routers.frontend-local.entrypoints=web"
      # HTTP→HTTPS redirect middleware
      - "traefik.http.middlewares.https-redirect.redirectscheme.scheme=https"

volumes:
  traefik_letsencrypt:
