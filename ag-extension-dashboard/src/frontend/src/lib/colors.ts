import { semanticTokens } from '@/theme/tokens';

/**
 * Imperative/JS styling palette for charts and toasts.
 * Sourced from the canonical design tokens (see src/theme/tokens.ts).
 */
export const CH_COLORS = {
  success: semanticTokens.status.success,
  error: semanticTokens.status.error,
  warning: semanticTokens.status.warning,
  blue: semanticTokens.chart.blue,
  purple: semanticTokens.chart.purple,
  cyber: semanticTokens.chart.cyber,
  green: semanticTokens.chart.green,
  gray: semanticTokens.chart.gray,
};
