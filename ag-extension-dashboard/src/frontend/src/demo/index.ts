/**
 * Demo mode — centralized module.
 *
 * Import demo data, guards, and the mode service from `@/demo` rather than
 * scattering `DEMO_*` imports and `isDemo` checks across the app.
 */
export * from './demoIds';
export * from './demoData';
export * from './demoMode';
export * from './useDemoMode';
